# prep/fixtures/ckpt_roundtrip_cuda.txt — recorded train→infer roundtrip
# generated ids for the CUDA from-scratch arm (prep/train_cuda_gate.rb).
# Split from the formerly-shared ckpt_roundtrip_baseline.txt: CPU and
# CUDA curves are byte-different by design, so their checkpoints (and
# generated ids) differ — the shared fixture only worked while the
# degenerate seed-0 init (toy#114) collapsed both to "7 7 7 …".
# Recorded 2026-07-25 (toy#114 init-quality reset: mixer-seeded stream).
ids: 1 2 3 297 146 591 225 369
