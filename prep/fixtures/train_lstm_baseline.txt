# prep/fixtures/train_lstm_baseline.txt — the toy#157 LSTM lane's default
# 5-step curve (libexec/toy-train-lstm with no LSTM_* env at all: 1 layer,
# hidden 64, features 24, T=64, 4 classes, delayed-cue task, all-chain,
# lr 0.02, NO warmup).
# The lane's fair cell is `--lr 0.02 --warmup 200 --steps 4000` and is
# spelled out wherever the lane's numbers are stated — it is deliberately
# NOT the default. Shipping its 200-step ramp as a default relabelled a
# consumer's 3-seed matrix within hours; see the LR constant in
# lib/toy/run/train_lstm.rb.
# Regenerate: STEPS=5 SEED=0 ./libexec/toy-train-lstm | grep '^step '
step 1: loss=1.3788046836853027
step 2: loss=1.432945728302002
step 3: loss=1.390499472618103
step 4: loss=1.3512670993804932
step 5: loss=1.4815672636032104
